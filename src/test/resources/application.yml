spring:
  application:
    name: porest

  mail:
    username: test@example.com
    password: testpassword
    host: smtp.example.com
    port: 587

  jpa:
    hibernate:
    properties:
      hibernate:
        default_batch_fetch_size: 100